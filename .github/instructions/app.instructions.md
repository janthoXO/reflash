---
applyTo: "mobile-app/**/*"
---
use react native with expo
use nativewind and react native reusable components for styling. use theme colors (primary, secondary, ...) instead of hardcoding colors
use drizzle for database queries.
always use pwd before executing commands
