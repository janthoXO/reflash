meta {
  name: uploadpdf
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/upload
  body: file
  auth: inherit
}

body:multipart-form {
  file: @file(testing.pdf)
}

body:file {
  file: @file(testing.pdf) @contentType(application/pdf)
}

settings {
  encodeUrl: true
  timeout: 0
}
