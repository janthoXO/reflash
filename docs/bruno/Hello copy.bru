meta {
  name: Hello copy
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/files?filenames=["test"]
  body: json
  auth: inherit
}

params:query {
  filenames: ["test"]
}

settings {
  encodeUrl: true
  timeout: 0
}
